/**
 * page-flip 随包分发 TS 源码的类型 stub（phase-14 oracle 对拍测试专用）。
 *
 * 测试直接 import 原版 FlipCalculation 作数值对拍；经 tsconfig paths 映射到本 stub，
 * 使 tsc 不编译 node_modules 内的原版源码（其自身在 strict 下有存量类型错误）。
 * 运行时由 vite/vitest 加载真实源码（vite 不读 tsconfig paths，互不影响）。
 */
export const FlipCalculation: any
export const FlipDirection: any
export const FlipCorner: any
